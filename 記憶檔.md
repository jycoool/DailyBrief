# 🧠 記憶檔 — 教我做的事 + 進度更新

> 這個檔案記錄「你教過我做的所有事」，以及每個工作的進度。
> 目的：讓我（Cline）在每個新 session 開頭讀這裡，就能找回過去進度、偏好與決策，
> 不用重新解釋。
>
> ⚙️ 使用規則：
> - 每完成一件事，就往下「## 進度更新」加一筆（最新在最上面）。
> - 「## 長期偏好 / 習慣」只記錄會重複用到的偏好。
> - 「## 我（你的）檔案地圖」指向所有重要檔案，避免迷路。

---

## 📝 進度更新（最新在上）

### 2026-09-08 — 接 Google Drive 自動備份（daily_brief 輸出）
- 新增 `Dashboard/drive_backup.py`：Google Drive 備份模組。
  - OAuth refresh token 認證（`--authorize` 一次性授權拿 token）。
  - `upload_file()` 用 multipart（MediaIoBaseUpload），中文檔名安全、不損壞（符合「base64Content 會損壞」踩坑）。
  - `_find_or_create_folder()` 依資料夾名稱自動查找/建立（符合「只能新建不能覆蓋」的 daily_brief 特性）。
- 接上 `daily_brief.py` 的 `save_output()`：新增 `try_backup_to_drive()`，設 `GOOGLE_DRIVE_ENABLED=1` 才啟用，失敗優雅降級不影響初判。
- `requirements.txt` 加入 google-api-python-client / google-auth / google-auth-oauthlib。
- ⚠️ 待使用者完成：跑一次 `python drive_backup.py --authorize` 拿 refresh token，設好環境變數。

### 2026-09-08 — 建立第二大腦 & 記憶檔
- 建立 `第二大腦.md`（專案分類總表：7 個專案 + 分析框架索引）。
- 建立 `記憶檔.md`（本檔）。
- 盤點 Dashboard 資料夾，確認既有知識結構：
  - `knowledge_project.md`（程式技術與待辦）
  - `knowledge_analysis.md`（分析歷史與驗證）
  - `knowledge_slim.md`（由 daily_brief 反哺）

### 2026-09-07 — 每日初判引擎收尾 / 日常產出
- 更新 `daily_brief.py`。
- 更新 `requirements.txt`、`snapshot.txt`。
- 產出盤後初判：`briefs/每日初判_盤後_2026-09-07_1853.md`。

### 2026-09-04 — 深度分析 HTML 渲染
- 新增 `render_deep_brief.py`，產出 `每日初判_盤後_2026-09-04_深度分析.html`。

### 2026-09-03 — 每日初判引擎自動化
- 完成 `README_每晚自動報告.md`（每晚 20:00 ET 常駐排程）。
- 完成 `run_daily_brief.sh`。
- 更新 `market_monitor.py`、`market_monitor_web.py`。
- 更新 `knowledge_analysis.md`（9/3 升息敘事退潮的第一波市場定價）。

### 2026-08-29 前後 — 輪動監控 / 知識檔
- 完成 `rotation_monitor.py`（9 組 RS 配對輪動監控）。
- 整理 `knowledge_project.md`（程式技術細節與待辦）。

---

## 🔁 長期偏好 / 習慣（我會遵守）

- **語言**：你用正體中文（繁體），回覆與文件用繁中。
- **分析框架**：核心分析框架存在 `核心分析框架.docx`，有「框架一~十」，並有「觀察哨清單」與「預測記錄與驗證表」。
- **誠實標記判斷**：分析判斷要誠實標記「是否判斷錯 / 市場先走一步」，不硬凹（見 knowledge_analysis.md 的風格）。
- **判讀紀律**：輪動要「三步確認」才動作（翻號→連續≥3日→廣度同向），翻號只是候選訊號。
- **輸出目的地**：機密報告最終要備份到 Google Drive（只可新建不可覆蓋）。
- **Drive 寫入規則**：只有 Cline 做的運作 + 股票（每日初判）報告才寫入「每日初判」資料夾（`GOOGLE_DRIVE_FOLDER_NAME=每日初判`）；其他備份不寫「每日初判」。**絕不在 Google Drive 做刪除、覆蓋、移動**——只新增寫入。
- **成本意識**：Claude API 用 `claude-sonnet-5`，每次初判約 US$0.05-0.15。

---

## 🗺️ 檔案地圖（重要檔案）

### 根目錄
- `/workspaces/agent-sandbox/第二大腦.md` — 專案分類總表
- `/workspaces/agent-sandbox/記憶檔.md` — 本檔
- `/workspaces/agent-sandbox/Dashboard/` — 主專案資料夾

### Dashboard 內
- `market_monitor.py` / `market_monitor_web.py` — 美股監控（現行版）
- `twstock_monitor.py` / `twstock_pro.py` — 台股監控
- `rotation_monitor.py` — 類股輪動
- `daily_brief.py` — 每日初判引擎（盤前/盤後）
- `drive_backup.py` — Google Drive 備份（選用，接在 daily_brief 後）
- `polymarket_monitor.py` — Polymarket
- `knowledge_project.md` — 程式技術細節 + 待辦
- `knowledge_analysis.md` — 分析歷史 + 驗證記錄
- `核心分析框架.docx` / `觀察哨清單.docx` / `預測記錄與驗證表.docx` — 分析三寶
- `watchlist.txt` — 觀察清單
- `briefs/` — 每日初判輸出
- `README_每日初判引擎.md` / `README_每晚自動報告.md` — 引擎說明

---

## 🎯 下一步建議（停在哪、下一步做啥）

- [ ] ⚠️ Drive 授權：跑 `python drive_backup.py --authorize` 拿 refresh token，設好
      `GOOGLE_DRIVE_REFRESH_TOKEN / CLIENT_ID / CLIENT_SECRET`，再設 `GOOGLE_DRIVE_ENABLED=1` 啟用。
- [ ] 決定 `daily_brief.py` 自動排程跑在哪（GitHub Actions vs Mac cron）——知識檔標記「尚未決定」。
- [ ] 更新 `daily_brief.py` 頂端 `KEY_DATES` 關鍵日期（過期的會自動略過）。
- [ ] 待辦清單見 `knowledge_project.md` 的「## 待辦」區（程式面 + 分析面）。